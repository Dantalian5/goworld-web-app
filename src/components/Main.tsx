import Filter from '@/components/Filter';
const Main = () => {
	return (
		<main className="px-4 py-6">
			<Filter />
			{/* --filter-input */}
			{/* --filter-drpdown */}
			{/* --country-card */}
		</main>
	);
};

export default Main;
